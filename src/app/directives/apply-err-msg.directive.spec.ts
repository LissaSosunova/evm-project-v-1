import { ApplyErrMsgDirective } from './apply-err-msg.directive';

describe('ApplyErrMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyErrMsgDirective();
    expect(directive).toBeTruthy();
  });
});
